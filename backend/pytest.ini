[pytest]
# Pin rootdir to backend/ and put it on sys.path so `import app...` resolves to
# backend/app rather than the /app namespace-package shadow (which broke collection).
pythonpath = .
testpaths = tests
asyncio_mode = auto
