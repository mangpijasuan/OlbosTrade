# OlbosTrade Full-App Audit — 2026-09-17 (rev 2)

Scope: complete re-audit of `main` branch.  
Prior audits: `AUDIT_2026-06.md`, `ARCHITECTURE_AUDIT.md` (rev 3, 2026-08-20).  
Method: static code reading, full backend test run (1891 tests), frontend test run (389 tests), TypeScript type-check, manual grep surveys across all routes, services, models, and frontend components.

---

## Test-suite health

| Suite | Before | After |
|---|---|---|
| Backend (`pytest`) | 1888 passed, **8 warnings** | **1891 passed, 3 warnings** |
| Frontend (`vitest`) | 389 passed, 0 warnings | 389 passed, 0 warnings |
| Frontend type check (`tsc --noEmit`) | Clean | Clean |

3 new backend tests (coordinator `stop()` and lifespan shutdown). The 3 remaining warnings are in third-party library code (`eventkit`, `starlette`) — none in application code.

---

## Items fixed this pass

### 1 · FastAPI `@app.on_event` deprecated — **FIXED**

**Severity:** P2

`app/main.py` used `@app.on_event("startup")` and `@app.on_event("shutdown")`, deprecated since FastAPI v0.95. Generated DeprecationWarning on every startup and test run.

**Fix:** `@asynccontextmanager async def lifespan(app)`, passed as `lifespan=lifespan` to `FastAPI(...)`. Startup and shutdown logic unchanged; only the registration mechanism changed.

Files: `backend/app/main.py`.

---

### 2 · `RuntimeWarning: coroutine never awaited` in kill-switch test — **FIXED**

**Severity:** P3 (test warning only)

`test_kill_switch_get_set` patched `kill_switch_service.reset` with a bare `AsyncMock()`. The production code calls `result.get("reset")` on the awaited result; when the mock returned a generic `MagicMock`, Python's mock machinery created an unawaited coroutine on `.get()` attribute access.

**Fix:** set `return_value={"reset": True}` on the reset mock and `return_value={}` on the engage mock.

Files: `backend/tests/test_trade_desk_routes.py`.

---

### 3 · `IBKRRequestCoordinator` worker tasks destroyed but pending in tests — **FIXED**

**Severity:** P3 (test noise only; no production effect)

Every test that imported `ibkr_coordinator` spawned its worker tasks via `start()`. When the per-test event loop was torn down, those tasks produced `Task was destroyed but it is pending!` messages.

**Fix:** added `IBKRRequestCoordinator.stop()` — cancels and awaits all worker tasks via `gather(..., return_exceptions=True)`. Wired into `_on_shutdown()`. The existing coordinator test fixture was updated to call `stop()` directly instead of cancelling manually.

**Added 3 tests:** `test_stop_cancels_and_awaits_all_workers`, `test_stop_is_idempotent`, and `test_lifespan_shutdown_calls_coordinator_stop`. The last test is a structural guard: if `_on_shutdown()` is ever refactored and the `ibkr_coordinator.stop()` call is dropped, the test fails loudly rather than silently leaking tasks. This was explicitly requested after the observation that the lifespan conversion (#1) and coordinator cleanup (#3) were separate changes that land in the same shutdown function — a botched conversion could silently lose the cleanup.

Files: `backend/app/broker/ibkr_coordinator.py`, `backend/app/main.py`, `backend/tests/test_ibkr_coordinator.py`.

---

### 4 · `TRADING_POLICY.md` claimed "Options spreads are SPY-only" — **FIXED**

**Severity:** P3 (docs drift) — but see note on risk surface below

The charter said: *"Options spreads are SPY-only today (the options spread engine is SPY-hardwired)"*. This was true in June 2026. Since then, `_run_options_scan` was rewritten to scan the full ~59-symbol equity watchlist, rank candidates by quality score, and route the highest-scoring spread.

**Important note on the scope of this fix:** this is a docs correction — it closes the paperwork gap. It does not answer whether the portfolio risk infrastructure was designed for 59 symbols or 1. See "Still open — multi-symbol risk surface" below for that analysis.

**Fix:** updated the "Supported assets → Status" paragraph.

Files: `TRADING_POLICY.md`.

---

### 5 · Two no-op stubs returned `{updated: true}` / `{toggled: true}` — **FIXED**

**Severity:** P2 (data contract lie: claimed success for operations that did nothing)

Two endpoints shared the same defect:

- `PUT /api/strategy/config` (`strategy.py:43`): returned `{"updated": True, ...}`
- `POST /api/paper-trade/toggle/{strategy}` (`paper_trade.py`): returned `{"toggled": True, ...}`

Both were shims for a pre–trading-mode design. The real mechanism is `POST /api/mode/set`. Neither had live frontend callers — `updateStrategyConfig` and `toggleStrategy` were defined in `client.ts` but never called from any component. Adding `require_api_key` to the toggle would have gated a lie and made it look deliberate and protected; it needed the 410, not auth.

**Fix:** both endpoints now return HTTP 410 Gone with a redirect message. Both dead client functions removed from `client.ts`.

Files: `backend/app/api/routes/strategy.py`, `backend/app/api/routes/paper_trade.py`, `frontend/src/api/client.ts`.

---

### 6 · Vestigial `POST /api/equity/signals/{signal_id}/approve` — **FIXED**

**Severity:** P2 (dead code with a wrong invariant; flagged open every prior audit)

`equity.py:136` stamped `approved_at` on a signal in the in-memory `_recent_signals` list. This never triggered execution. The real approval path (`POST /api/trade-desk/approve/{id}`) is authenticated and executes. Nothing in the frontend called the equity route.

**Fix:** endpoint removed; now-unused `timezone` import removed.

Files: `backend/app/api/routes/equity.py`.

---

### 7 · `POST /api/mode/set` unauthenticated — **FIXED**

**Severity:** P2

`POST /api/mode/set` (and `POST /api/mode/reset-to-balanced`) had no `require_api_key` dependency. Trading mode controls the system-wide risk aggressiveness: Conservative uses 1%/trade, Scalper uses 3× (40 trades/day cap). Any caller who could reach the FastAPI layer could flip the system from Conservative to Scalper.

**The nginx basic auth question:** `DASH_USER`/`DASH_PASS` defaults to empty in every provided `.env.example`. The Caddy snippet and `docker-entrypoint.sh` both print a loud warning when `DASH_USER` is unset. So the nginx layer is **not present by default** — `require_api_key` (checked against `SECRET_KEY`) is the correct second layer, not a belt over nginx.

`SECRET_KEY` is also optional (defaults to empty), which means on a default install with neither set, `mode/set` is open. But the same is true of `execution-mode`, `approve`, `manual-trade`, and every other auth'd route — the risk posture is consistent: both empty → open, both set → double-protected. Adding `require_api_key` to `mode/set` brings it into that consistent pattern.

**Fix:** added `Depends(require_api_key)` to both `POST /api/mode/set` and `POST /api/mode/reset-to-balanced`.

Files: `backend/app/api/routes/trading_mode.py`.

---

## Still open (not changed this pass)

### Auth / unauthenticated routes

| Sev | Route | Notes |
|---|---|---|
| P1 | `POST /api/trade-desk/kill-switch` engage | Intentionally unauthenticated at FastAPI layer (emergency stop should never require a token lookup); nginx Basic Auth applies in production when configured. By design. |
| P2 | `POST /api/risk/kill-switch/engage` | Same: UI-facing engage path, protected by app-level session (require_session) when AUTH_ENABLED=true. Lower-risk than trade-desk kill-switch because it flows through the same kill_switch_service. |
| P2 | `POST /api/risk/reconciliation/run` | Forces a full position reconciliation against the broker. No execution path but can be CPU/network intensive. Carries no `require_api_key`. |
| P3 | `POST /api/alerts/rules`, `DELETE /api/alerts/rules/{id}`, watchlist management (`/api/intel/watchlists`) | No `require_api_key`. Lower-risk (no execution path) but allow injecting alert rules or modifying the scan watchlist. |

---

### Multi-symbol risk surface expansion — OPEN (analysis, no fix applied)

The options scanner expansion from SPY/QQQ to ~59 symbols raises a concrete question: were the position risk limits designed for 1 symbol or 59?

**What holds:**
- `max_concurrent_positions = 5` (config default) is a hard portfolio-level cap enforced in `check_execution_portfolio()` — it doesn't matter if 59 or 1 symbol qualified; only 5 positions can be open simultaneously.
- Single-underlying concentration (25% of portfolio per underlying) is a hard block at both `risk_manager.approve_trade()` and `execution_portfolio_gate`. Scanning 59 symbols doesn't create 59 concentrated positions.
- Per-trade risk sizing scales off portfolio value × `risk_per_trade_pct` (1–3% depending on mode), not off watchlist size.
- The daily trade cap (balanced: 5/day hard max) bounds how many new entries per day regardless of how many symbols qualified.

**What does not hold — sector concentration enforcement:**
`risk_manager.approve_trade()` checks sector concentration as **warn-only** (appends a flag, never blocks). The Nasdaq-100 watchlist is heavily Technology; a full 5-position portfolio with NVDA, MSFT, AAPL, AMD, and META would reach near-100% Technology exposure with no hard block from `approve_trade`. The execution gate (`execution_portfolio_gate.check_execution_portfolio`) does have a hard sector block, so on the live execution path sector concentration *is* enforced. But the `approve_trade` path (used by `UnifiedRiskEngine.check()` and potentially by preview/evaluate endpoints) still only warns. This inconsistency between the two paths should be unified — either both hard-block or both warn, with an explicit policy choice recorded.

**What does not exist — correlation enforcement:**
`compute_correlation_clusters()` in `portfolio_engine.py` (threshold 0.70) computes correlation clusters for display at `/api/portfolio/heat` and for rotation review. It is **not wired into any execution gate**. Five Nasdaq-100 tech positions with pairwise correlation 0.85 would clear every check and open. The max-5-positions cap limits the damage (5 correlated positions are not 59), but the system has no mechanism to prefer uncorrelated entries when multiple symbols qualify in the same scan cycle.

**Recommended fix (not applied here):** add correlation cluster membership as a soft flag on `PortfolioGateResult` — not a hard block (computing it requires 30+ historical bars, adding latency to the execution path), but surfaced in the execution log so the operator can see "this position added to a correlated cluster."

---

### `UnifiedRiskEngine` / `EmotionGuard` are dead code — OPEN

`app/services/unified_risk.py` contains `UnifiedRiskEngine` (wraps GuardrailEngine + RiskManager + EmotionGuard) and `EmotionGuard` (pauses trading after 4 consecutive losses or tilt score > 0.75). The module docstring says "Every order must call `UnifiedRiskEngine.check()` before touching the broker." The equity.py docstring says "All signals pass through the UnifiedRiskEngine before execution." Neither is true.

**Actual execution path:** `_execute_signal` → `check_execution_portfolio` (execution_portfolio_gate) → `RiskManager.approve_trade` directly. `UnifiedRiskEngine.check()` is never called. `EmotionGuard.record_trade_result()` is never called. The consecutive-loss pause is a tested feature that cannot fire.

The gap matters: 4 consecutive losses → tilt score 1.0 → 8-hour pause is the kind of circuit-breaker that matters specifically when things go wrong fast. It being untested-in-production is the failure mode it was built to prevent.

**Recommended fix:** either (a) wire `UnifiedRiskEngine.check()` into `_execute_signal` before Stage 2b, and wire `record_trade_result()` into `_poll_fills` after each close, or (b) remove `unified_risk.py` and document the decision. Option (a) is the intent; option (b) is honest about the current state.

---

### XGBoost model — deliberately not deployed; heuristic is the correct current state

**Status:** `ml/model_registry/signal_scorer_v1.pkl` exists on the host (bind-mounted read-only) but the file is absent from the repository history (`.gitignore` excludes `*.pkl`). The model was assessed and deliberately **not deployed** because it has no skill: r² = −0.173, directional_accuracy = 50.68% on n_val=442, which is below the mean baseline. Shipping it would have replaced an honest heuristic with a model that predicts worse than naive. The deployment gap was accidentally protecting the desk.

The heuristic is the **intentional current state** — not a gap. The signal scorer's `/api/health/detail` endpoint reports `scoring_mode: "heuristic"`, `loaded: false`, `usable: false` along with the model's validation metrics verbatim.

**The connection to measured signal performance:** if the system's realized win rate is ~7% target-hit (from the `signal_outcomes` label study), that measurement is against the **heuristic scorer's** confidence gate, not an XGBoost model. A future model that meets the skill threshold changes the signal selection function, which changes the win-rate baseline. Until then, performance metrics measure the heuristic.

**Prerequisite for retraining:** label space was incomplete until the expired-at-20-bars window first fired in mid-September 2026. The earliest credible label set now exists. Revisit when there are ≥ 6 months of resolved, uncensored labels and non-zero options history.

---

### Other open items

| Sev | Area | Location | Issue |
|---|---|---|---|
| P2 | `main.py` monolith | `backend/app/main.py` | 3159 lines, 49 functions. Background scheduler, scan orchestration, signal handling, portfolio-state assembly, indicator math, and health endpoints in one file. Hard to audit for new regressions. Unchanged since 2026-07-16 when it was first flagged. |
| P3 | Bear-put spread incomplete | `options_decision_engine.py:86`, `strategy_engine.py` | `bear_put` is handled in the decision engine but absent from the background scanner's strategy class map — it can never be selected by autopilot. TRADING_POLICY.md correctly marks it as not yet implemented (roadmap). No runtime risk. |

---

## Architecture health snapshot

### Genuinely sound

- **Single execution path.** AUTOPILOT routes exclusively through `_execute_signal`. Verified unchanged.
- **Regime stabilizer.** Hysteresis (3 consecutive readings to adopt, CRISIS instant) working. Prevents machine-speed strategy-hopping.
- **Kill-switch rehydration.** Survives restarts.
- **Risk constants unified.** `portfolio_greeks.py` imports from `RiskManager`. Not duplicated.
- **Position identity.** `(underlying, asset_type)` keying in place; duplicate guard and reconciliation both use it.
- **Options pricing.** Live IBKR chain used when available; Black-Scholes documented fallback.
- **P&L math.** Correct multipliers and direction conventions for both asset classes.
- **Live tenure guard.** Blocks live-capital execution until 90 days paper + 20 closed trades. Fail-closed.
- **Account mode guard.** Startup verifies gateway account type vs `IBKR_TRADING_MODE`. Fail-closed.
- **Portfolio gate.** Step 8 in execution path checks max-positions, single-underlying concentration (25% hard block), sector concentration (40% hard block), and portfolio heat. Working.

### Paper-safe but not yet live-capital-ready

- `UnifiedRiskEngine` / `EmotionGuard` not wired (consecutive-loss pause is inactive).
- Correlation clustering display-only (not in execution gate).
- No trained XGBoost model (heuristic is intentional; prerequisite for retraining now met).

---

## Process note

All findings verified directly in code. Tests run clean before and after all changes.
