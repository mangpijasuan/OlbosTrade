# ═══════════════════════════════════════════════════════════════════════════════
# OlbosQuant — Hetzner production stack
#
# Runs ALONGSIDE the existing olbos app on the same server.
# Caddy (already running in olbos) handles HTTPS for both apps.
# OlbosQuant joins the olbos Docker network so Caddy can reach it.
#
# Usage — on the server inside /opt/olbosquant:
#   bash deploy/hetzner/up.sh
# ═══════════════════════════════════════════════════════════════════════════════

services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: olbosquant-backend
    restart: unless-stopped
    env_file:
      - backend/.env.prod
    environment:
      DATABASE_URL: postgresql+asyncpg://olbosquant:${OLBOSQUANT_DB_PASSWORD}@olbosquant-db:5432/olbosquantdb
    depends_on:
      olbosquant-db:
        condition: service_healthy
    networks:
      - olbosquant_internal
      - olbos_default        # joins existing Caddy network
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8000/api/guardrails/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "1.5"
          memory: 1500M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ""
        VITE_WS_URL: ""
    container_name: olbosquant-frontend
    restart: unless-stopped
    environment:
      - BACKEND_URL=http://olbosquant-backend:8000
      # Set DASH_USER/DASH_PASS in backend/.env.prod to password-protect the whole
      # dashboard + API (HTTP Basic Auth). Leave unset to serve open (NOT advised
      # when exposed by public IP).
      - DASH_USER=${DASH_USER:-}
      - DASH_PASS=${DASH_PASS:-}
    # Publish a host port for direct IP access (http://<server-ip>:8080) when no
    # domain is set up. Caddy (when used) still reaches it over the internal
    # network; this just additionally exposes it on the host.
    ports:
      - "8080:3000"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - olbosquant_internal
      - olbos_default        # joins existing Caddy network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  olbosquant-db:
    image: postgres:16-alpine
    container_name: olbosquant-db
    restart: unless-stopped
    environment:
      POSTGRES_USER:     olbosquant
      POSTGRES_PASSWORD: ${OLBOSQUANT_DB_PASSWORD:?set OLBOSQUANT_DB_PASSWORD in backend/.env.prod}
      POSTGRES_DB:       olbosquantdb
    volumes:
      - olbosquant_pgdata:/var/lib/postgresql/data
    networks:
      - olbosquant_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U olbosquant -d olbosquantdb"]
      interval: 5s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

volumes:
  olbosquant_pgdata:

networks:
  olbosquant_internal:
    driver: bridge
  olbos_default:
    external: true
    name: docker_default    # the network Caddy is already on (created by the olbos app)
